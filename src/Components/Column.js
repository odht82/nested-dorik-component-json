import Item from './Item';

const Column = ({ className, content }) => {
    return (
        // <div className={className}>
        {/* { */ }
        // content.map(item => (
        // <Item
        // key={item.id}
        // heading={item.titleType}
        // type={item.type}
        // className={item.id}
        // attr={item.attr}
        // content={item.content}
        // form={item.form}
        // />))
        // }
        // </div>
    );
};

export default Column;